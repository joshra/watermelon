# 單機靜態網頁版西瓜遊戲完整規格

## 1. 專案目標

建立一個可直接在瀏覽器中遊玩的單機靜態網頁版西瓜遊戲。

本專案優先順序如下：

1. 先完成清楚、可執行的完整規格文件。
2. 再依規格生成 AI 視覺素材。
3. 最後才進行遊戲系統實作與測試。

遊戲需可在桌機與手機瀏覽器中運行，不依賴後端、資料庫或線上服務。完成後應能透過本機靜態伺服器或直接開啟網頁檔案遊玩。

## 2. 核心體驗

玩家從畫面上方選擇水平位置，讓水果掉入容器中。相同等級的水果碰撞後合成下一級水果，水果越大分數越高。當水果堆疊超過危險線並維持一段時間後，遊戲結束。

體驗重點：

- 操作簡單，滑鼠、鍵盤、觸控皆可玩。
- 水果數量、名稱、大小與合成關係要清楚。
- 視覺素材必須直接使用 AI 生成，且整體風格一致。
- 畫面應明亮、乾淨、可愛，但不要看起來像拼湊素材。
- 第一版重點是完整可玩，不優先做排行榜、音效或複雜特效。

## 3. 平台與技術

- 類型：單機靜態網頁遊戲。
- 檔案形式：HTML、CSS、JavaScript、圖片素材。
- 建議物理引擎：Matter.js，放入專案本機檔案，不使用 CDN。
- 繪製方式：單一 HTML Canvas。
- 後端：無。
- 資料庫：無。
- 建置流程：第一版不需要。
- 網路需求：遊戲運行時不需要網路。

建議檔案結構：

```text
/
  index.html
  styles.css
  game.js
  SPEC.md
  assets/
    fruits/
      fruit-sheet.png
      cherry.png
      strawberry.png
      grape.png
      orange.png
      apple.png
      pear.png
      peach.png
      pineapple.png
      cantaloupe.png
      coconut.png
      watermelon.png
  vendor/
    matter.min.js
```

## 4. 水果規格

遊戲固定使用 11 種水果。水果由小到大排列如下：

| 等級 | 水果 | 英文代號 | 半徑建議 | 合成後 |
|---:|---|---|---:|---|
| 1 | 櫻桃 | cherry | 16 | 草莓 |
| 2 | 草莓 | strawberry | 22 | 葡萄 |
| 3 | 葡萄 | grape | 28 | 橘子 |
| 4 | 橘子 | orange | 35 | 蘋果 |
| 5 | 蘋果 | apple | 42 | 梨子 |
| 6 | 梨子 | pear | 50 | 桃子 |
| 7 | 桃子 | peach | 59 | 鳳梨 |
| 8 | 鳳梨 | pineapple | 69 | 哈密瓜 |
| 9 | 哈密瓜 | cantaloupe | 80 | 椰子 |
| 10 | 椰子 | coconut | 92 | 西瓜 |
| 11 | 西瓜 | watermelon | 106 | 無 |

規則：

- 只有同等級水果碰撞才會合成。
- 兩顆同等級水果合成後，產生下一級水果。
- 西瓜是最高級水果，不再合成。
- 每次合成應播放簡單視覺反饋，例如短暫放大、閃光或分數提示。
- 掉落佇列只產生前 5 級水果，避免一開始就掉落過大的水果。

## 5. 分數規格

分數以合成為主。

建議分數：

| 合成產物 | 分數 |
|---|---:|
| 草莓 | 2 |
| 葡萄 | 4 |
| 橘子 | 8 |
| 蘋果 | 16 |
| 梨子 | 32 |
| 桃子 | 64 |
| 鳳梨 | 128 |
| 哈密瓜 | 256 |
| 椰子 | 512 |
| 西瓜 | 1024 |

顯示項目：

- 目前分數。
- 下一顆水果。
- 目前持有水果。
- 遊戲結束後的最終分數。

第一版不做永久最高分。若後續要加入最高分，可使用 `localStorage`。

## 6. 操作規格

桌機操作：

- 滑鼠移動：控制水果掉落的水平位置。
- 滑鼠點擊：投放目前水果。
- 左右方向鍵：移動投放位置。
- 空白鍵：投放目前水果。
- `P`：暫停或繼續。
- `R`：重新開始。
- `F`：全螢幕切換。

手機操作：

- 手指水平拖曳：控制水果掉落位置。
- 輕觸畫面：投放目前水果。
- 頁面不得因觸控操作而捲動或縮放。

投放限制：

- 每次投放後需有短暫冷卻時間，建議 450ms。
- 遊戲暫停或結束時不可投放。
- 投放位置不得超出容器左右邊界。

## 7. 畫面與 UI

畫面結構：

- 中央為遊戲容器。
- 上方顯示分數與下一顆水果。
- 容器內上方有危險線。
- 遊戲結束時顯示結束面板與重新開始按鈕。

Canvas 建議邏輯尺寸：

- 寬度：480。
- 高度：720。
- 容器底部與左右兩側為物理牆。
- 危險線建議位於 `y = 125`。

響應式規格：

- 桌機：Canvas 置中，旁邊可保留簡潔資訊區。
- 手機：直式優先，Canvas 依螢幕寬度等比例縮放。
- 任何尺寸下文字、按鈕與水果預覽不得重疊。

## 8. AI 素材生成規格

所有水果素材必須直接使用 AI 生成，不使用網路下載的現成水果圖示，也不直接複製原作素材。

### 8.1 整體風格

整組水果需維持一致的整體風格：

- 風格：可愛、柔和、拋光感的 3D clay render 或 toy-like mobile game icon。
- 光線：柔和棚拍光，不要強烈陰影。
- 形狀：圓潤、飽滿、容易辨識。
- 色彩：明亮但不螢光，飽和度一致。
- 材質：同一組水果都像同一套遊戲素材，而不是不同來源拼貼。
- 視角：正面或微俯視，所有水果角度一致。
- 邊緣：清楚、乾淨，適合縮小後顯示。
- 背景：生成時使用純色去背背景，後續處理成透明 PNG。

避免：

- 不同水果使用不同繪畫風格。
- 有些是照片、有些是卡通。
- 有文字、標籤、浮水印。
- 強烈投影或背景地面。
- 臉部表情或角色化五官。
- 過度複雜導致小尺寸看不清楚。

### 8.2 生成方式

優先方式：

1. 一次生成完整 11 種水果 sprite sheet。
2. 使用同一個 prompt 描述整體風格。
3. 以固定格線排版，方便後續切圖。
4. 背景使用單一 chroma key 顏色，方便去背。
5. 切出 11 個透明 PNG 後再進入遊戲。

若一次生成品質不佳，第二方案：

1. 先生成一張「水果素材風格參考圖」。
2. 再依此風格逐一生成 11 種水果。
3. 每次生成 prompt 必須重複同一套風格限制。
4. 所有成品需人工檢查一致性，不一致者重生。

### 8.3 AI 生成 Prompt 範本

```text
Create one cohesive sprite sheet containing exactly 11 separate fruit game pieces
for a Suika-style merging puzzle game:
cherry, strawberry, grape cluster, orange, apple, pear, peach, pineapple,
cantaloupe, coconut, watermelon.

Style: consistent cute polished 3D clay-rendered fruit icons, rounded forms,
soft studio lighting, friendly casual mobile game style.

Composition: clean grid layout, one fruit centered in each cell, generous padding,
no overlapping, no labels, no numbers, no text.

Background: perfectly flat solid chroma-key background, no shadows, no gradients,
no texture, no floor plane, no reflections.

Constraints: exactly 11 fruits, all in the same visual style, crisp readable
silhouettes, no characters, no faces, no watermark, no text.
```

### 8.4 素材驗收標準

每顆水果素材需符合：

- PNG 透明背景。
- 水果主體完整，沒有被裁切。
- 小尺寸顯示仍能辨認。
- 與其他水果維持一致光線、材質、視角與精緻度。
- 沒有文字、浮水印、背景殘留。
- 實際遊戲半徑與圖像尺寸比例自然。

## 9. 物理與合成規格

物理行為：

- 使用重力讓水果自然下落。
- 水果彼此碰撞時應有彈性，但不可過度彈跳。
- 容器左右與底部有牆壁。
- 水果不應穿牆或掉出畫面。

合成行為：

- 兩個同等級水果碰撞後，合成位置取兩者中心平均。
- 原兩顆水果移除，新增下一級水果。
- 合成需避免同一碰撞在同一幀被重複處理。
- 合成後水果可繼續與其他水果連鎖合成。

建議物理參數：

- 重力：`1.0`。
- 摩擦：中等。
- 彈性：低到中等，避免水果亂飛。
- 密度：水果越大可略高，但第一版可統一。

## 10. 遊戲狀態

遊戲狀態至少包含：

- `ready`：初始或重開後等待開始。
- `playing`：遊戲進行中。
- `paused`：暫停。
- `gameover`：遊戲結束。

狀態轉換：

- `ready` 投放第一顆水果後進入 `playing`。
- `playing` 按 `P` 進入 `paused`。
- `paused` 按 `P` 回到 `playing`。
- `playing` 觸發結束條件進入 `gameover`。
- `gameover` 按重新開始回到 `ready` 或直接 `playing`。

遊戲結束條件：

- 任一水果在穩定狀態下高於危險線。
- 且此狀態連續維持約 2 秒。
- 新水果剛落下時短暫穿越危險線不應立即判定失敗。

## 11. 測試與除錯介面

為了方便自動測試，需提供：

```js
window.render_game_to_text = function () {
  // return JSON string
};

window.advanceTime = function (ms) {
  // deterministic simulation step
};
```

`render_game_to_text()` 需回傳：

- 座標系統說明。
- 遊戲狀態。
- 分數。
- 目前水果。
- 下一顆水果。
- 危險線位置。
- 場上水果清單：等級、名稱、位置、半徑、速度。
- 是否暫停。
- 是否遊戲結束。

測試情境：

- 首次載入畫面正常。
- 滑鼠移動與點擊可投放水果。
- 鍵盤左右鍵與空白鍵可操作。
- 觸控事件可操作且頁面不捲動。
- 兩顆相同水果可合成下一級。
- 分數正確增加。
- 下一顆水果顯示正確更新。
- 暫停後物理停止。
- 重新開始後清空場面與分數。
- 水果超過危險線後進入遊戲結束。
- 桌機與手機尺寸下畫面無重疊。

## 12. 第一版不包含項目

第一版暫不包含：

- 音效。
- 背景音樂。
- 本機最高分。
- 多語系切換。
- 關卡模式。
- 線上排行榜。
- 帳號系統。
- 分享功能。
- 複雜粒子特效。

這些可作為第二版功能。

## 13. 實作順序

建議依下列順序實作：

1. 完成並確認本規格文件。
2. 生成 AI 水果 sprite sheet。
3. 去背並切出 11 個水果 PNG。
4. 建立靜態網頁骨架。
5. 接入 Matter.js 物理容器。
6. 實作水果投放。
7. 實作同級合成。
8. 實作分數與下一顆水果。
9. 實作遊戲結束與重新開始。
10. 加入桌機與手機操作。
11. 加入測試介面。
12. 執行瀏覽器測試與截圖檢查。

## 14. 驗收標準

完成品需符合：

- 可在本機瀏覽器單機遊玩。
- 無後端、無資料庫、無線上素材依賴。
- 有 11 種水果，名稱與合成順序符合規格。
- 素材由 AI 生成，且整組水果風格一致。
- 桌機與手機皆可操作。
- 合成、分數、下一顆水果、遊戲結束與重開都能正常運作。
- 畫面乾淨，不重疊，不破版。
- 瀏覽器 console 沒有錯誤。
- `render_game_to_text()` 與 `advanceTime(ms)` 可供自動測試使用。

